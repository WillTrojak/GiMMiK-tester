# GiMMiK Tester

Simple performance tester for GiMMiK kernels that makes use of PyFR's
framework for generating and calling kernels.

The `mats` directory contains the PyFR matrices taken from the libxsmm
samples.

The benchmark code was mostly written by Freddie Witherden.